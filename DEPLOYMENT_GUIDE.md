# 🚀 Инструкция по развертыванию бота @SlotSignalsBot на Railway

## Быстрый старт (5 минут)

### Шаг 1: Подготовка GitHub репозитория

```bash
# Если еще не инициализирован Git
git init
git add .
git commit -m "Initial commit: SlotSignalsBot"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/slotbot.git
git push -u origin main
```

### Шаг 2: Создание проекта на Railway

1. Откройте https://railway.app
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Авторизуйтесь с GitHub
5. Выберите репозиторий `slotbot`
6. Нажмите **"Deploy"**

### Шаг 3: Настройка переменных окружения

1. В Railway откройте ваш проект
2. Перейдите на вкладку **"Variables"**
3. Добавьте переменные:

```
BOT_TOKEN=6933838057:AAFO-gNcgx93PBdPIZORxP6oKQ2F5Ti3AMg
TELEGRAM_CHAT_ID=534928819
DATABASE_PATH=/tmp/bot.db
SOFT_GATE_UNLOCK_TIME=300
SOFT_GATE_CHECK_INTERVAL=10
ENABLE_POSTBACK=true
LOG_LEVEL=INFO
```

4. Нажмите **"Save"**

### Шаг 4: Запуск бота

Railway автоматически начнет развертывание. Дождитесь статуса **"Success"** ✅

---

## 📋 Полная инструкция

### Архитектура

```
GitHub Repository
    ↓
Railway Project
    ├→ Bot Service (worker)
    │  └ main.py (polling)
    │
    └→ Database
       └ SQLite (/tmp/bot.db)
```

### Что происходит при развертывании:

1. **Railway клонирует репозиторий** с GitHub
2. **Устанавливает зависимости** из requirements.txt
3. **Запускает Procfile** (worker: python main.py)
4. **Бот начинает polling** и слушает сообщения
5. **Фоновые задачи** проверяют разблокировку сигналов каждые 10 сек

### Файлы для развертывания:

| Файл | Назначение |
|------|-----------|
| `main.py` | Точка входа бота |
| `requirements.txt` | Зависимости Python |
| `Procfile` | Команда запуска для Railway |
| `.env.example` | Пример переменных окружения |
| `app/` | Исходный код приложения |

---

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|-----------|---------|----------------------|
| `BOT_TOKEN` | Токен Telegram бота | Обязательно |
| `TELEGRAM_CHAT_ID` | ID чата админа | Обязательно |
| `DATABASE_PATH` | Путь к БД | `/tmp/bot.db` |
| `SOFT_GATE_UNLOCK_TIME` | Время разблокировки (сек) | 300 |
| `SOFT_GATE_CHECK_INTERVAL` | Интервал проверки (сек) | 10 |
| `ENABLE_POSTBACK` | Включить постбеки | true |
| `LOG_LEVEL` | Уровень логирования | INFO |

### Структура приложения

```
app/
├── core/
│   ├── config.py       # Конфигурация
│   ├── logger.py       # Логирование
│   └── __init__.py
├── database/
│   ├── db.py           # Слой БД
│   ├── models.py       # Модели данных
│   └── __init__.py
├── handlers/
│   ├── start.py        # Обработчик /start
│   ├── signals.py      # Обработчик /signals
│   ├── text_handler.py # Обработчик текста
│   └── vip_upsell.py   # VIP апсел
├── services/
│   ├── soft_gate_service.py      # Soft gate система
│   ├── subscription.py           # Подписка
│   ├── autoresponder_service.py  # Автоответчик
│   ├── retention_service.py      # Retention
│   └── postback_pro_service.py   # Постбеки
├── locales/
│   └── i18n.py         # Локализация
└── __init__.py
```

---

## 📊 Мониторинг

### Логи в Railway

1. Откройте проект на Railway
2. Перейдите на вкладку **"Logs"**
3. Смотрите логи работы бота в реальном времени

### Ожидаемые логи при запуске:

```
2026-02-02 14:00:00 - __main__ - INFO - 🚀 Запуск PRO-бота для арбитража трафика...
2026-02-02 14:00:01 - app.database.db - INFO - ✅ Таблицы БД инициализированы
2026-02-02 14:00:01 - __main__ - INFO - ✅ БД инициализирована
2026-02-02 14:00:01 - __main__ - INFO - ✅ Сервисы инициализированы
2026-02-02 14:00:01 - __main__ - INFO - ✅ Фоновые задачи запущены
2026-02-02 14:00:01 - __main__ - INFO - ✅ Бот готов к работе!
2026-02-02 14:00:01 - __main__ - INFO - 📡 Запуск polling...
```

---

## 🧪 Тестирование

### Тест 1: Проверить что бот работает

1. Откройте Telegram
2. Найдите @SlotSignalsBot
3. Нажмите **/start**
4. Должны увидеть приветственное сообщение

### Тест 2: Проверить логирование

1. Напишите боту любое сообщение
2. Откройте логи в Railway
3. Должны увидеть логи обработки сообщения

### Тест 3: Проверить soft gate

1. Нажмите **/signals**
2. Нажмите кнопку "Открыть казино"
3. Должны увидеть таймер
4. Через 5 минут сигналы разблокируются

---

## 🐛 Troubleshooting

### Проблема: Бот не отвечает

**Решение:**
1. Проверьте логи в Railway
2. Убедитесь что `BOT_TOKEN` правильный
3. Перезагрузите сервис в Railway

### Проблема: Ошибка "ModuleNotFoundError"

**Решение:**
1. Проверьте что все файлы загружены на GitHub
2. Убедитесь что `requirements.txt` содержит все зависимости
3. Перезагрузите Railway

### Проблема: БД не создается

**Решение:**
1. Railway использует `/tmp/` для временных файлов
2. БД создается автоматически при первом запуске
3. Если ошибка - проверьте права доступа в логах

### Проблема: Сигналы не разблокируются

**Решение:**
1. Проверьте что `SOFT_GATE_CHECK_INTERVAL` = 10
2. Проверьте что `SOFT_GATE_UNLOCK_TIME` = 300
3. Проверьте логи фоновых задач

---

## 📈 Масштабирование

### Для большого количества пользователей:

1. **Используйте Redis вместо MemoryStorage:**
   ```python
   from aiogram.fsm.storage.redis import RedisStorage
   storage = RedisStorage.from_url("redis://...")
   ```

2. **Добавьте PostgreSQL вместо SQLite:**
   ```python
   DATABASE_URL = "postgresql://user:pass@host/db"
   ```

3. **Используйте Webhook вместо Polling:**
   ```python
   await dp.start_webhook(
       listening_host="0.0.0.0",
       listening_port=8080,
       webhook_path="/webhook"
   )
   ```

---

## 🔐 Безопасность

### Рекомендации:

1. **Не коммитьте .env файлы** - используйте .env.example
2. **Используйте переменные окружения** для всех секретов
3. **Включите двухфакторную аутентификацию** на GitHub
4. **Регулярно обновляйте зависимости**

---

## 📞 Поддержка

- **Документация aiogram**: https://docs.aiogram.dev/
- **Railway docs**: https://docs.railway.app/
- **Telegram Bot API**: https://core.telegram.org/bots/api

---

## ✅ Чек-лист развертывания

- [ ] Код загружен на GitHub
- [ ] Railway проект создан
- [ ] Переменные окружения установлены
- [ ] Бот развернут и работает
- [ ] Логи показывают "✅ Бот готов к работе!"
- [ ] Бот отвечает на /start
- [ ] Soft gate система работает
- [ ] Постбеки обрабатываются

**Готово! Бот работает 24/7 для всех пользователей! 🎉**
